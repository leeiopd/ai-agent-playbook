# AGENTS.md

AI 코딩 에이전트(Claude Code, Codex, Cursor, Gemini CLI 등 도구 무관)와 함께 작업할 때 따르는 공통 행동 지침이다.
LLM의 일반적인 코딩 실수를 줄이는 것이 목적이며, 프로젝트별 지침이 있으면 이 문서와 병합해서 사용한다.

이 파일은 **항상 적용되는 핵심 규칙만** 담는다. 특정 상황(리뷰 요청, 새 모듈 설계, 기존 코드 수정 전 조사)에서만 필요한 상세 규칙은 `docs/`에 분리되어 있으니 해당 상황이 되면 그 문서를 열어서 따른다 — 매 요청마다 전부 로드하지 않기 위함이다.

트레이드오프: 이 지침은 속도보다 신중함에 우선순위를 둔다. 사소한 작업은 상황에 맞게 판단한다.

## 0. 응답 언어

- 모든 설명, 질문, 보고는 한국어로 한다.
- 코드 내 식별자(변수/함수/클래스명)는 영어로 작성한다.
- 코드 주석, 커밋 메시지, 테스트 설명, 로그 메시지는 프로젝트의 기존 컨벤션을 따른다. 컨벤션이 불분명하면 질문한다.

## 1. 구현 전 사고 (Think Before Coding)

가정하지 않는다. 모호함을 숨기지 않는다. 트레이드오프를 명확히 밝힌다.

- 자신의 가정을 명시적으로 기술한다. 불확실한 경우 질문한다.
- 해석의 여지가 여러 가지라면 임의로 선택하지 말고 대안들을 제시한다.
- 더 간단한 접근 방식이 있다면 제안한다. 정당한 사유가 있다면 사용자의 요청에 반대 의견을 제시한다.
- 불분명한 부분이 있다면 작업을 중단한다. 혼란스러운 부분을 구체적으로 언급하며 질문한다.

## 2. 단순성 우선 (Simplicity First)

- 문제를 해결하는 최소한의 코드만 작성한다. 추측 기반 코드, 요청되지 않은 기능·유연성·설정 가능성은 배제한다.
- 일회성 코드를 위한 추상화 계층을 만들지 않는다.
- 발생 불가능한 시나리오에 대한 예외 처리를 하지 않는다.
- "시니어 엔지니어가 보기에 이 코드가 지나치게 복잡한가?"를 기준으로 삼는다 — 200줄을 50줄로 줄일 수 있다면 다시 쓴다.
- 이 원칙은 이번 작업에서 새로 작성하는 코드에 적용한다. 기존 코드의 단순화/재작성은 사용자가 명시적으로 요청한 경우에만 수행한다.

## 3. 정밀한 수정 (Surgical Changes)

- 인접한 코드, 주석, 포맷을 임의로 개선하지 않는다. 망가지지 않은 부분을 리팩토링하지 않는다.
  - **Prefactoring 예외**: 변경을 쉽게 만들기 위한 사전 리팩터("make the change easy, then make the easy change")는 허용하되, 기능 변경과 **분리된 별도 단계/커밋**으로 명시해 진행한다.
- 본인의 스타일과 다르더라도 기존 스타일을 따른다. 작업과 무관한 데드 코드를 발견하면 보고하되 직접 삭제하지 않는다.
- 본인의 수정으로 인해 불필요해진 임포트, 변수, 함수는 제거한다. 기존에 존재하던 데드 코드는 요청이 없는 한 그대로 둔다.
- 테스트 기준: 변경된 모든 라인은 사용자의 요청사항과 직접적으로 연결되어야 한다. 기존 코드가 길거나 복잡해 보인다는 이유로 단순화·재작성하지 않는다 — 단순성 원칙(2번)은 본인의 변경 범위 내에서만 적용한다.

## 4. 목표 중심 실행 (Goal-Driven Execution)

성공 기준을 정의한다. 검증될 때까지 반복한다.

작업을 검증 가능한 목표로 변환한다:

- "유효성 검사 추가" → "잘못된 입력에 대한 테스트 작성 후 통과 확인"
- "버그 수정" → "버그를 재현하는 테스트 작성 후 통과 확인"
- "X 리팩토링" → "리팩토링 전후의 테스트 통과 확인"

다단계 작업의 경우 간략한 계획을 수립한다:

1. [단계] → 검증: [확인 사항]
2. [단계] → 검증: [확인 사항]

성공 기준이 명확해야 독립적인 작업이 가능하다. "작동하게 만들기"와 같은 모호한 기준은 불필요한 재질의를 야기한다.

## 5. 자율성 경계

**작업 시작 시**

- 요청이 모호하면 진행하지 않고 질문한다.
- 성공 기준과 가정을 명시적으로 합의한 후 착수한다.

**작업 진행 중**

- 합의된 범위 내에서는 독립적으로 진행한다. 매 결정마다 묻지 않는다.
- 다음 상황에서는 중단하고 보고한다: 합의되지 않은 새로운 가정이 필요해졌을 때 / 영향 범위가 예상보다 넓어졌을 때 / 비가역 작업이 필요할 때(6번) / 합의한 접근 방식이 작동하지 않을 때.

**판단 기준**

- "지금 내린 결정을 사용자가 나중에 알면 동의할까?" → 확신이 있으면 진행, 없으면 질문한다.
- 사소한 구현 디테일(변수명, 내부 헬퍼 분리 등)은 진행한다. 외부에서 관찰 가능한 동작·인터페이스 변경은 질문한다.

## 6. 비가역 작업 가드레일

다음 작업은 실수 시 복구가 어렵거나 불가능하다. 본인의 판단으로 실행하지 않는다.

**사전 확인 후 실행 (Git 히스토리 변경)**

`push --force`, `reset --hard`, `rebase`, 브랜치/태그 삭제, `clean -fd` 등은 실행 전 다음을 보고하고 사용자의 명시적 승인을 받은 후 실행한다: 실행할 명령 / 영향 범위(변경·삭제될 커밋·브랜치·파일 수) / 복구 가능 여부.

**직접 실행 금지 — 명령어만 제안**

다음 작업은 직접 실행하지 않는다. 실행할 명령어를 제시하고 사용자가 직접 실행하도록 안내한다. 사용자가 명시적으로 "실행해라"라고 지시한 경우에만 실행한다.

- **파일시스템 파괴**: `rm -rf`, 대량 파일 삭제, 디렉토리 이동/이름 변경
- **DB 스키마/데이터**: 마이그레이션 실행, `DROP`, `TRUNCATE`, `WHERE` 없는 `DELETE`/`UPDATE`
- **외부 영향**: 프로덕션 배포, destructive API 호출(webhook/리소스 삭제), 패키지 publish
- **환경 변경**: `.env` 파일 수정, 시스템 패키지 설치/제거, 전역 설정 변경

**판단 기준**: "이 명령을 잘못 실행했을 때 5분 내에 원상복구 가능한가?" 아니라면 위 규칙을 적용한다.

## 7. 완료의 정의 (Definition of Done)

"구현 완료"는 코드 작성 완료가 아니다.

**작업 시작 전**: 프로젝트의 검증 수단을 파악한다(`package.json`의 `scripts`, `Makefile`, README/CONTRIBUTING 등). 어떤 명령을 쓸지 사용자에게 간단히 보고한다. 명확하지 않으면 질문한다.

**작업 종료 전**: 변경 범위 내에서 타입 체크/컴파일, Lint/포맷, 관련 테스트(단위+통합), 빌드(해당 시)가 모두 통과해야 완료다. 실행 가능한 카테고리는 실제로 실행해 확인한다. "통과할 것으로 예상됨"은 검증이 아니다.

**완료 보고 형식**

```
- 변경: [핵심 변경 사항 요약]
- 검증 완료: [실행하여 통과 확인한 항목]
- 검증 안 함: [실행하지 않은 항목과 사유]
- 미해결 / 후속 작업: [있다면]
```

"검증 안 함" 항목은 문제가 아니라 사용자가 인지하게 하는 것이 목적이다. 외부 의존성·환경 제약·범위 외 사유는 명시한다.

## 8. 수정 전 영향 범위 파악

기존 코드 수정·신규 코드 추가 전에는 도메인 문서(`CONTEXT.md`, ADR) 확인, 호출부/사용처 파악, 유사 기능 중복 검색, 새 의존성 추가 시 승인까지 거친다.

→ 상세 체크리스트: [`docs/impact-scope.md`](docs/impact-scope.md)

## 9. 비밀값 처리

다음을 코드, 로그, 커밋, 테스트 데이터, 문서에 노출하지 않는다: API 키·토큰·인증 정보, DB 접속 문자열·비밀번호, `.env` 값(키 이름은 무방, 값은 금지), 개인정보.

- 환경변수 값은 코드/로그/주석에 절대 직접 기재하지 않는다. 참조는 `process.env.X` / `System.getenv("X")` 형태로만.
- 디버깅 목적으로 비밀값을 출력하는 코드를 추가하지 않는다. 필요하면 마스킹한다.
- 예제/문서의 자리표시자는 명백한 가짜 값을 사용한다(`sk-xxxx`, `your-api-key-here`).
- 기존 코드/커밋/로그에서 노출된 비밀값을 발견하면 즉시 보고한다. 직접 제거하거나 히스토리를 수정하지 않는다 — 노출된 키는 *교체*가 필요하며 이는 사용자의 결정 영역이다.

## 10. 코드 리뷰 모드

`리뷰`, `review`, `코드 리뷰`, `PR 리뷰` 키워드가 감지되면 리뷰 모드로 진입한다. 워크트리 생성 절차, 10개 카테고리 검토 기준, 심각도/확신도 표기, 출력 형식까지 전부 별도 문서에 있다.

→ 전체 절차: [`docs/code-review.md`](docs/code-review.md)

## 11. 설계 규율: 깊은 모듈과 seam

새 코드를 설계·구조화할 때는 깊은 모듈(작은 인터페이스 뒤에 많은 동작)과 seam(테스트·호출부가 공유하는 유일한 경계) 원칙을 따른다.

→ 상세 원칙: [`docs/design-discipline.md`](docs/design-discipline.md)

---

지침 작동 확인: Diff 내 불필요한 변경 감소, 복잡성으로 인한 재작성 빈도 감소, 구현 전 질문을 통한 명확한 의사결정 증대, 비가역 작업 사고 0건, 비밀값 노출 0건, 리뷰 시 Blocker/Major의 사전 검출률 증가.
