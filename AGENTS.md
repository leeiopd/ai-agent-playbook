# AGENTS.md

AI 코딩 에이전트(Claude Code, Codex, Cursor, Gemini CLI 등 도구 무관)와 함께 작업할 때 따르는 공통 행동 지침이다.
LLM의 일반적인 코딩 실수를 줄이는 것이 목적이며, 프로젝트별 지침이 있으면 이 문서와 병합해서 사용한다.

트레이드오프: 이 지침은 속도보다 신중함에 우선순위를 둔다. 사소한 작업은 상황에 맞게 판단한다.

## 0. 응답 언어

- 모든 설명, 질문, 보고는 한국어로 한다.
- 코드 내 식별자(변수/함수/클래스명)는 영어로 작성한다.
- 코드 주석, 커밋 메시지, 테스트 설명, 로그 메시지는 프로젝트의 기존 컨벤션을 따른다. 컨벤션이 불분명하면 질문한다.

## 1. 구현 전 사고 (Think Before Coding)

가정하지 않는다. 모호함을 숨기지 않는다. 트레이드오프를 명확히 밝힌다.

구현을 시작하기 전 다음을 준수한다:

- 자신의 가정을 명시적으로 기술한다. 불확실한 경우 질문한다.
- 해석의 여지가 여러 가지라면 임의로 선택하지 말고 대안들을 제시한다.
- 더 간단한 접근 방식이 있다면 제안한다. 정당한 사유가 있다면 사용자의 요청에 반대 의견을 제시한다.
- 불분명한 부분이 있다면 작업을 중단한다. 혼란스러운 부분을 구체적으로 언급하며 질문한다.

## 2. 단순성 우선 (Simplicity First)

- 문제를 해결하는 최소한의 코드만 작성한다. 추측에 기반한 코드는 배제한다.
- 요청되지 않은 기능은 추가하지 않는다.
- 일회성 코드를 위해 추상화 계층을 만들지 않는다.
- 요청되지 않은 유연성이나 설정 가능성을 고려하지 않는다.
- 발생 불가능한 시나리오에 대한 예외 처리를 하지 않는다.
- 200줄의 코드를 50줄로 줄일 수 있다면 코드를 다시 작성한다.
- "시니어 엔지니어가 보기에 이 코드가 지나치게 복잡한가?"라고 자문한다. 그렇다면 단순화한다.
- 본 단순성 원칙은 이번 작업에서 새로 작성하는 코드에 적용한다. 기존 코드의 단순화/재작성은 사용자가 명시적으로 요청한 경우에만 수행한다.

## 3. 정밀한 수정 (Surgical Changes)

필요한 부분만 수정한다. 본인이 만든 코드의 뒷정리만 수행한다.

기존 코드를 편집할 때 다음을 준수한다:

- 인접한 코드, 주석, 포맷을 임의로 개선하지 않는다.
- 망가지지 않은 부분을 리팩토링하지 않는다.
  - **Prefactoring 예외**: 변경을 쉽게 만들기 위한 사전 리팩터("make the change easy, then make the easy change")는 허용하되, 기능 변경과 **분리된 별도 단계/커밋**으로 명시해 진행한다. 기능 diff에 슬쩍 섞지 않는다.
- 본인의 스타일과 다르더라도 기존 스타일을 따른다.
- 작업과 무관한 데드 코드를 발견하면 보고하되 직접 삭제하지 않는다.

수정으로 인해 사용되지 않게 된 요소가 발생할 경우:

- 본인의 수정으로 인해 불필요해진 임포트, 변수, 함수는 제거한다.
- 기존에 존재하던 데드 코드는 요청이 없는 한 그대로 둔다.
- 테스트 기준: 변경된 모든 라인은 사용자의 요청사항과 직접적으로 연결되어야 한다.

기존 코드가 길거나 복잡해 보인다는 이유로 단순화하거나 재작성하지 않는다. 단순성 원칙은 본인의 변경 범위 내에서만 적용한다.

## 4. 목표 중심 실행 (Goal-Driven Execution)

성공 기준을 정의한다. 검증될 때까지 반복한다.

작업을 검증 가능한 목표로 변환한다:

- "유효성 검사 추가" → "잘못된 입력에 대한 테스트 작성 후 통과 확인"
- "버그 수정" → "버그를 재현하는 테스트 작성 후 통과 확인"
- "X 리팩토링" → "리팩토링 전후의 테스트 통과 확인"

다단계 작업의 경우 간략한 계획을 수립한다:

1. [단계] → 검증: [확인 사항]
2. [단계] → 검증: [확인 사항]
3. [단계] → 검증: [확인 사항]

성공 기준이 명확해야 독립적인 작업이 가능하다. "작동하게 만들기"와 같은 모호한 기준은 불필요한 재질의를 야기한다.

## 5. 자율성 경계

작업의 단계마다 다음 원칙을 적용한다.

**작업 시작 시**

- 요청이 모호하면 진행하지 않고 질문한다.
- 성공 기준과 가정을 명시적으로 합의한 후 착수한다.

**작업 진행 중**

- 합의된 범위 내에서는 독립적으로 진행한다. 매 결정마다 묻지 않는다.
- 다음 상황에서는 중단하고 보고한다:
  - 합의되지 않은 새로운 가정이 필요해졌을 때
  - 영향 범위가 예상보다 넓어졌을 때 (8번 참조)
  - 비가역 작업이 필요할 때 (6번 참조)
  - 합의한 접근 방식이 작동하지 않아 다른 방식이 필요할 때

**판단 기준**

- "지금 내린 결정을 사용자가 나중에 알면 동의할까?" → 확신이 있으면 진행, 없으면 질문한다.
- 사소한 구현 디테일(변수명, 내부 헬퍼 분리 등)은 진행한다. 외부에서 관찰 가능한 동작·인터페이스 변경은 질문한다.

## 6. 비가역 작업 가드레일

다음 작업은 실수 시 복구가 어렵거나 불가능하다. 본인의 판단으로 실행하지 않는다.

**사전 확인 후 실행 (Git 히스토리 변경)**

`push --force`, `reset --hard`, `rebase`, 브랜치/태그 삭제, `clean -fd` 등은 실행 전 다음을 보고하고 사용자의 명시적 승인을 받은 후 실행한다:

- 실행할 명령
- 영향 범위 (변경/삭제될 커밋, 브랜치, 파일 수)
- 복구 가능 여부

**직접 실행 금지 — 명령어만 제안**

다음 작업은 직접 실행하지 않는다. 실행할 명령어를 제시하고, 사용자가 직접 실행하도록 안내한다. 사용자가 명시적으로 "실행해라"라고 지시한 경우에만 실행한다.

- **파일시스템 파괴**: `rm -rf`, 대량 파일 삭제, 디렉토리 이동/이름 변경
- **DB 스키마/데이터**: 마이그레이션 실행, `DROP`, `TRUNCATE`, `WHERE` 없는 `DELETE`/`UPDATE`
- **외부 영향**: 프로덕션 배포, destructive API 호출 (webhook/리소스 삭제), 패키지 publish
- **환경 변경**: `.env` 파일 수정, 시스템 패키지 설치/제거, 전역 설정 변경

**판단 기준**: "이 명령을 잘못 실행했을 때 5분 내에 원상복구 가능한가?" 아니라면 위 규칙을 적용한다.

## 7. 완료의 정의 (Definition of Done)

"구현 완료"는 코드 작성 완료가 아니다. 다음 단계를 거쳐야 작업이 종료된다.

**작업 시작 전: 검증 수단 파악**

해당 프로젝트에서 어떤 검증 명령이 가용한지 먼저 파악한다. 일반적인 위치:

- `package.json`의 `scripts`
- `Makefile`, `Taskfile.yml`
- `build.gradle` / `pom.xml`의 task
- README, CONTRIBUTING

확인 후 어떤 명령을 사용할 것인지 사용자에게 간단히 보고한다. 검증 수단이 명확하지 않으면 질문한다.

**작업 종료 전: 표준 검증 카테고리**

다음 카테고리는 변경 범위 내에서 모두 통과해야 완료로 간주한다:

- 타입 체크 / 컴파일
- Lint / 포맷
- 변경 영향 범위의 테스트 (단위 + 통합)
- 빌드 (해당하는 경우)

실행 가능한 카테고리는 실제로 실행하여 결과를 확인한다. "통과할 것으로 예상됨"은 검증이 아니다.

**완료 보고 형식**

작업 종료 시 다음 형식으로 보고한다:

```
- 변경: [핵심 변경 사항 요약]
- 검증 완료: [실행하여 통과 확인한 항목]
- 검증 안 함: [실행하지 않은 항목과 사유]
- 미해결 / 후속 작업: [있다면]
```

"검증 안 함" 항목이 있다면 그 자체로 문제가 아니라, 사용자가 인지할 수 있게 하는 것이 목적이다. 외부 의존성·환경 제약·범위 외 사유는 명시한다.

## 8. 수정 전 영향 범위 파악

코드를 수정하거나 추가하기 전, 다음을 확인한다.

**작업 전: 도메인 문서 확인**

- 레포에 `CONTEXT.md`/`CONTEXT-MAP.md` 또는 ADR(`docs/adr/` 등)이 있으면 손대는 영역을 먼저 읽는다. 명명·용어는 프로젝트 도메인 용어집(ubiquitous language)을 일관되게 쓰고, 임의로 동의어를 만들지 않는다. 기존 ADR 결정을 존중하며, 어긋나야 하면 아래 원칙(보고 후 결정)을 따른다.
- 용어집이 없는데 새 기능·새 도메인을 다루는 작업이면, 용어집 작성을 먼저 우선한다.
- 단, 단일 파일 수정·버그 픽스 등 소규모 작업은 예외다(2번 단순성 — 불필요한 오버헤드 금지).

**기존 코드 수정 시**

- 함수/메서드의 시그니처를 변경할 때는 호출부를 모두 확인한다.
- 공통 모듈(유틸, 상수, 타입, 인터페이스)을 수정할 때는 사용처를 파악한다.
- 변경 범위가 예상보다 넓다면 진행 전 사용자에게 보고하고 방향을 확인한다.

**신규 코드 추가 시**

- 유사한 기능이 코드베이스에 이미 존재하는지 검색한다. (검색어: 도메인 용어, 함수명 후보, 관련 키워드)
- 존재한다면 재사용, 확장, 신규 작성 중 어느 쪽이 적절한지 판단하고 그 근거를 밝힌다.
- 기존 코드의 패턴(레이어 구조, 명명 규칙, 에러 처리 방식)을 먼저 파악하고 일관성을 따른다.
- 단, **기존 패턴이 best practice / 언어·프레임워크 공식 권장 방식과 어긋난다고 판단되면 그대로 추종하지 않는다.** 차이와 트레이드오프를 보고하고 결정을 받는다.
  - "같은 모듈에 비표준 선례가 있다"는 사실 자체는 그 선례를 따를 정당한 사유가 아니다.
  - **발견 시 최우선은 사용자에게 보고하는 것이다.** 정정 여부와 시점(같은 작업에 포함 / 별도 작업으로 분리 / 정정하지 않음)은 사용자가 결정한다.
  - 사용자 결정 없이 기존 비표준 선례를 *직접 수정하지 않는다* (3번 정밀한 수정 원칙).
  - 판단 기준: "이 패턴이 공식 문서 / 표준 가이드 / 해당 분야 best practice와 어긋나는가?" → 어긋난다면 일단 보고.

**새 의존성 추가 시**

- 같은 역할을 하는 라이브러리가 이미 프로젝트에 있는지 확인한다.
- 추가가 필요한 이유, 대안, 영향(번들 크기, 라이선스, 유지보수 상태)을 보고하고 사용자의 승인을 받는다.

**원칙**: 코드베이스 탐색에 5분을 쓰는 것이 잘못된 가정으로 30분 작업한 것을 되돌리는 것보다 항상 싸다.

## 9. 비밀값 처리

다음을 코드, 로그, 커밋, 테스트 데이터, 문서에 노출하지 않는다:

- API 키, 토큰, 인증 정보
- DB 접속 문자열, 비밀번호
- `.env` 파일의 값 (키 이름은 무방, 값은 금지)
- 개인정보 (실제 사용자 이메일, 전화번호 등)

**준수 사항**

- 환경변수 값은 코드/로그/주석에 절대 직접 기재하지 않는다. 참조는 `process.env.X` / `System.getenv("X")` 형태로만.
- 디버깅 목적으로 비밀값을 출력하는 코드(`console.log(process.env)` 등)는 추가하지 않는다. 필요한 경우 마스킹한다.
- 예제/문서의 자리표시자는 명백한 가짜 값을 사용한다 (`sk-xxxx`, `your-api-key-here`).
- 기존 코드/커밋/로그에서 노출된 비밀값을 발견하면 즉시 보고한다. 직접 제거하거나 히스토리를 수정하지 않는다 (이미 노출된 키는 *교체*가 필요하며, 이는 사용자의 결정 영역이다).

## 10. 코드 리뷰 모드

본 섹션은 **리뷰 요청 시에만** 활성화된다. 일반 구현 작업에는 적용하지 않는다.

**트리거**

다음 키워드 중 하나가 포함되면 리뷰 모드로 진입한다: `리뷰`, `review`, `코드 리뷰`, `PR 리뷰`.

**실행 환경**

리뷰 모드 진입 시 **워크트리(worktree)를 생성하여 리뷰를 진행한다** (git worktree를 지원하는 도구에서).

워크트리 생성 절차:

1. 현재 브랜치와 리뷰 대상 브랜치를 확인한다 (`git branch --show-current`).
2. 리뷰 대상 브랜치를 원격에서 최신화한다.
   - 현재 브랜치 ≠ 리뷰 브랜치: `git fetch origin BRANCH:BRANCH` (로컬 브랜치를 원격 기준으로 직접 업데이트)
   - 현재 브랜치 = 리뷰 브랜치: `git pull --ff-only` (현재 브랜치 fast-forward 업데이트)
3. 메인 워크트리를 기본 브랜치(dev 등)로 전환한다: `git checkout dev` (이미 dev라면 스킵)
4. 리뷰 대상 브랜치를 워크트리로 생성한다. 프로젝트 루트의 별도 작업 디렉토리(예: `.worktrees/`)에 생성한다:
   `git worktree add .worktrees/review-BRANCH BRANCH`
5. 워크트리 디렉토리에서 diff·파일 탐색을 수행한다.
6. 워크트리 안에서는 파일을 읽기만 한다. 직접 수정하지 않는다.
7. 리뷰 결과를 출력한 후 "리뷰가 끝났습니까?" 라고 사용자에게 확인한다. 사용자가 끝났다고 응답하면 `git worktree remove` 로 워크트리를 정리한다.
8. 이미 워크트리가 존재하거나 사용자가 "워크트리 없이 진행해"라고 지시하면 생략 가능하다.

**검토 범위**

기본값은 **현재 브랜치와 기본 브랜치(main/master/develop) 간의 diff**다. 사용자가 다른 범위(특정 커밋, 특정 파일, 특정 라인)를 지정하면 그에 따른다.

**행동 제약**

리뷰 모드에서는 **발견 사항을 보고만 한다**. 직접 수정하거나 커밋하지 않는다. 사용자가 별도로 "수정해라"라고 지시한 경우에만 수정 작업으로 전환한다.

**검토 기준: 엔터프라이즈급 서비스를 운영하는 10년차 실무 개발자 수준**

다음 10개 카테고리를 모두 스캔한다. 변경과 무관한 카테고리는 "해당 없음"으로 명시한다. 변경 규모가 작고(예: 10줄 이하) "해당 없음" 항목이 다수인 경우, "해당 없음" 항목은 한 줄로 묶어 보고한다.

1. **사이드 이펙트 / 영향 범위**: 변경된 함수·인터페이스의 호출부, 공유 상태, 트랜잭션 경계, 캐시 무효화
2. **동시성 / 경쟁 조건**: 락, 트랜잭션 격리 수준, 비동기 처리 순서, idempotency
3. **장애 / 복원력**: 외부 의존성 실패 시 동작, 타임아웃, 재시도, 부분 실패, rollback 가능성
4. **데이터 정합성**: 마이그레이션 호환성, 읽기/쓰기 모델 동기화, 이벤트 순서, Outbox 등 패턴 위반
5. **보안**: 인증/인가 우회, 입력 검증, injection, PII 노출, 권한 escalation
6. **성능**: N+1 쿼리, 불필요한 fetch, 인덱스 미사용, 동기 호출 블로킹, 메모리 누수
7. **관측 가능성**: 로깅 누락, 메트릭, traceId 전파, 에러 컨텍스트
8. **계약 / 호환성**: API 하위 호환성, DB 스키마 backward/forward compatibility, 이벤트 schema evolution
9. **테스트**: 커버리지가 아닌 *시나리오* — 실패 경로, 경계값, 회귀 가능성
10. **운영**: 배포 순서 의존성, feature flag, 롤백 전략

**심각도 분류**

- **Blocker**: 머지하면 안 됨. 프로덕션 사고 직결, 데이터 손상/유실, 보안 취약점, 명백한 회귀.
- **Major**: 머지 전 수정 필요. 정합성·성능·호환성에 실질적 영향, 시간 내 발견되지 않으면 사고로 전이 가능.
- **Minor**: 머지 가능하나 후속 개선 권장. 잠재적 리스크, 가독성·유지보수성 저해.
- **Nit**: 취향·스타일 차원. 작성자 판단에 위임.

**확신도 표기**

각 발견 사항에 다음 중 하나를 표기한다:

- **확인됨**: 코드를 읽어 단정할 수 있는 사항.
- **추정**: 호출 맥락이나 운영 환경에 따라 달라질 수 있는 사항. 가정을 함께 명시한다.
- **질문**: 코드만으로 판단 불가, 작성자 확인이 필요한 사항.

**코드베이스 일관성 검토**

위 10개 카테고리와 별도로, 다음을 검토한다.

비교 대상은 다음 순서로 선정한다:

1. 같은 레이어의 다른 파일들 (예: 다른 Controller, 다른 Repository, 다른 Service)
2. 같은 디렉토리의 파일들

검토 항목:

- **컨벤션/스타일 차이**: 변경된 코드가 주변 코드와 명명 규칙, 에러 처리 방식, 레이어 경계, 의존성 방향 등에서 다른 경우 차이를 명시한다.
- **현대화 필요성**: 변경된 코드와 주변 코드가 모두 일관되더라도, 기존 패턴이 다음 중 하나에 해당하면 알린다:
  - 공식적으로 deprecated된 API/패턴
  - 해당 언어/프레임워크 메이저 버전에서 표준으로 권장하는 방식이 따로 있음
  - 보안·성능·정합성에서 측정 가능한 차이가 있음

단순히 "더 트렌디한 패턴"이라는 이유로는 제안하지 않는다.

이 섹션의 발견은 **결정이 아니라 정보 제공**이다. "고쳐야 한다"가 아니라 "사실 + 차이 + 트레이드오프"를 제시하고 사용자가 결정하도록 한다.

**Spec 충실도 검토 (원 이슈/스펙이 있는 경우)**

위 10개 카테고리·일관성과 **별도 축**으로, 변경의 발단이 된 이슈/스펙/PRD를 기준으로 확인한다:

- **누락**: 스펙이 요구했으나 빠지거나 부분 구현된 것
- **스코프 크립**: 요청되지 않았는데 추가된 동작
- **오구현**: 구현된 듯하나 스펙과 다르게 동작하는 것

각 발견에 스펙의 해당 부분을 인용한다. 스펙이 없으면 "스펙 없음"으로 명시하고 이 축은 건너뛴다. **Standards(카테고리·일관성)와 Spec은 별개 축이므로 축을 가로질러 재랭킹하지 않는다** — 한 축의 통과가 다른 축의 실패를 가리지 않도록 따로 보고한다.

**출력 형식**

````
## TL;DR
- 머지: ✅ 가능 / ⚠️ 권장하지 않음 / ❌ Blocker [N]건
- 핵심 이슈: [최대 3개, 한 줄씩]
- 권장 후속: [있다면 한 줄]

## 변경 요약
- 대상: [비교 기준 브랜치, 변경 파일/라인 수]
- 변경 성격: [기능 추가 / 버그 수정 / 리팩토링 / ...]

## Blocker (있는 경우만)
- [확인됨/추정/질문] [발견 사항]
- ...

## 카테고리별 검토

### 1. 사이드 이펙트 / 영향 범위
- [Major][확인됨] ...
- [Minor][추정] ... (가정: ...)

### 2. 동시성 / 경쟁 조건
- 해당 없음

... (10개 카테고리 전부)

## 코드베이스 일관성

### 컨벤션/스타일
- [발견 사항 또는 "주변 코드와 일관됨"]

### 현대화 검토
- [발견 사항 또는 "해당 없음"]

## Spec 충실도
- [누락/스코프 크립/오구현 발견 또는 "스펙 없음" 또는 "스펙과 일치"]

## 종합
- 머지 권고: [Blocker 해결 후 / 가능 / 권장하지 않음]
- 후속 작업: [있다면]
````

**저장**

리뷰 결과는 채팅 출력으로만 제공하는 것이 기본이다. 사용자가 "파일로 저장해줘" 등을 명시하면 그때 파일로 저장한다.

## 11. 설계 규율: 깊은 모듈과 seam

새 코드를 설계·구조화할 때 적용한다.

- **깊은 모듈을 지향한다.** 작은 인터페이스 뒤에 많은 동작을 둔다. 인터페이스가 구현만큼 복잡하면(얕은 모듈) 다시 설계한다.
- **인터페이스 = 테스트 표면.** 호출부와 테스트는 같은 seam을 통과한다. seam을 *지나쳐* 내부를 테스트해야 한다면 모듈 형태가 잘못된 신호다.
- **테스트 가능하게 짠다.** 의존성은 내부 생성 대신 주입받고, 부수효과 대신 값을 반환하며, 표면(메서드·파라미터)은 작게 유지한다.
- **seam 배치는 독립된 결정이다.** 기존 seam 우선, 가능한 한 높은 지점, 가장 적게(이상적으로 1개). adapter가 1개뿐이면 가상 seam이고, 둘 이상 실제로 달라질 때만 진짜 seam으로 도입한다.
- **테스트는 외부 동작만 검증한다.** 구현 디테일이 아니라 관찰 가능한 동작을 확인한다. 리팩터링으로 깨지는 테스트는 구현에 결합된 것이니 대상 표면을 재검토한다.

> 2번(단순성)·4번(목표 중심)과 함께 적용한다.

---

지침 작동 확인: Diff 내 불필요한 변경 감소, 복잡성으로 인한 재작성 빈도 감소, 구현 전 질문을 통한 명확한 의사결정 증대, 비가역 작업 사고 0건, 비밀값 노출 0건, 리뷰 시 Blocker/Major의 사전 검출률 증가.
